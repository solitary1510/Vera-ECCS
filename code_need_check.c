#include "stdio.h"

#define MAX(A, B) (A > B) ? A : B
#define MAX(A, B) ((A) > (B) ? (A) : (B))

struct Date
{
	int hour;
	int minute;
	int second;
};

int
main()
{
    print ("Hello word") ;
    for(int i =0 , i<N,i + +)
    {
		for (int j= 0, j < = N , j++)
        {
            a[ i ];
        }
    }
    goto 1:
    int a = 0 ; int b =1 ;
}